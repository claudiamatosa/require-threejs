/**
 * # Settings
 * 
 * This module allows easy management of the app's settings. It contains
 * initial settings, that may be overriden as the app changes status.
 */
define(function() {
    return {
        containerId: 'tutorial-container',
        width: window.innerWidth,
        height: window.innerHeight  
    };
});